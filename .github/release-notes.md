## QuickQC Release Notes Source

Release notes are generated automatically from `CHANGELOG.md` by:

`scripts/generate-release-notes.sh`

The GitHub workflow uses that generated file when creating tag releases.

To keep release notes correct, always add a matching section to `CHANGELOG.md` before tagging.
